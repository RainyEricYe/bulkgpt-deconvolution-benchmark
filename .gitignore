# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.so

# Data (too large for git; download via data/prepare/download_all.sh)
data/**/*.h5ad
data/**/*.h5
data/**/*.rds
data/**/*.rda
data/real_bulk/
!data/manifest.yaml
!data/prepare/

# Pretrained models (downloaded separately via data/prepare/download_pretrained.sh)
pretrained_models/

# Checkpoints (too large for git; download via data/prepare/download_pretrained.sh)
checkpoints/

# Internal planning
plan.md

# Results (generated on-demand by running benchmarks)
results/

# Test configs (auto-generated with machine-specific paths)
tests/configs/

# Containers (too large for git; build via containers/build_all.sh)
containers/sif/*.sif
containers/sif/*.sif.*
containers/build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.bak
*.orig

# OS
.DS_Store
Thumbs.db

# Environment
.env

# Logs (auto-generated by benchmark runs)
logs/

# Test outputs (generated on-demand by running tests)
tests/output/

# Package
*.egg-info/
build/
dist/

# Claude IDE config
.claude/

# Decode runtime artifacts
methods/decode/src/data/decode_run/
methods/decode/src/save_models/
bulk.csv
repo/

# ── Publication release exclusions ──────────────────────────────
# Internal backups & drafts (never publish)
bak/
paper.ds.bak/
ara-output/
logs/
scheduler/
*.pid
*.o
*.e

# Internal docs / analysis drafts
docs/remote_analysis_*
docs/superpowers/
docs/r1_complete_case_sensitivity.md
docs/science_advances_comprehensive_review_20260806.md

# Internal monitoring / status files (republish a cleaned summary instead)
resource_usage.md
results_summary.md
used.cml
note

# Decode per-tissue intermediate features (data repo)
methods/decode/src/data/
methods/decode/src/save_models/

# Test large fixtures (data repo)
tests/enriched_input.h5
tests/data/
tests/mcpcounter/
tests/output/

# Containers: upstream clones (data repo / buildable)
containers/recide/

# slurm / node-run artifacts
*.sh.o*
*.sh.e*

# weights: only real checkpoint binaries are excluded (scripts are tracked)
weights/**/*.ckpt
weights/**/*.pt
weights/**/*.pkl
weights/**/models.ckpt

# Cross-dataset experiment outputs (regenerate via tests/cross_dataset_pbmc.py)
tests/cross_dataset_pbmc/
tests/loo_within_fold/

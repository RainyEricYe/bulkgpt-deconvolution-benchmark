name: TOAST
mode: container
container_image: containers/sif/toast.sif
host_env: bulkgpt
timeout: 7200
entry:
  run: methods/toast/run.py
test_configs:
  default: tests/configs/toast/default.yaml
description: >
  TOAST — TOAST deconvolution.
  Container-based method running inside toast.sif Apptainer image.

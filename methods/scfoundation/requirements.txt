torch>=2.1.0
scanpy>=1.9.1
anndata>=0.8.0
numpy<2
pandas>=1.3
scikit-learn>=1.0
scipy>=1.7
pyyaml
tqdm

# scFoundation model code is embedded in the worktree's src/bulkgpt/model/.
# No separate scfoundation package is required at runtime.
# However, the following data files from the scFoundation repo are needed:
#   - scfoundation_src/OS_scRNA_gene_index.19264.tsv
#   - data/scfoundation_ensembl_to_scfpos.json
#   - data/scfoundation_symbol_to_ensembl.json
# These are included with the worktree checkout.

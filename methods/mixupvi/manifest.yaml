name: MixupVI
mode: container
container_image: containers/sif/mixupvi.sif
host_env: bulkgpt
timeout: 7200
entry:
  run: methods/mixupvi/run.py
test_configs:
  default: tests/configs/mixupvi/default.yaml
description: >
  MixupVI — Mixup variational inference deconvolution.
  Container-based method running inside mixupvi.sif Apptainer image.

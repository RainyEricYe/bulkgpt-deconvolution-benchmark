data:
  celltype_col: cell_type
model:
  stack_checkpoint: weights/stack/bc_large_aligned.ckpt
  genelist_path: weights/stack/basecount_1000per_15000max.pkl
  embed_dim: 1600
  hidden_dim: 512
  progressive: true
  n_progressive_layers: 3
training:
  n_pseudo_bulk: 5000
  epochs: 30
  batch_size: 64
  lr: 0.00007
  weight_decay: 0.00001
  seed: 42
paths:
  output_dir: tests/output/stack/progressive3

name: bulkformer_random_mean_pool
mode: conda
conda_env: bulkformer
entry:
  run: run.py
timeout: 7200
requires_gpu: true
description: "Random init BulkFormer + full encoder + mean pooling + RidgeCV (control)"

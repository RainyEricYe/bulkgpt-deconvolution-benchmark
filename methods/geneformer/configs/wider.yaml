dataset:
  batch_col: null
  celltype_col: cell_type
  data_path: tests/data/sdy67.h5
  n_hvg: 200
model:
  cell_emb_style: mean
  deconv_dropout: 0.2
  deconv_hidden_dim: 256
  deconv_n_layers: 3
  max_seq_len: 2048
paths:
  checkpoint_dir: tests/output/geneformer/wider
  gene_cache: weights/transcriptformer/tf_sapiens/gene_cache/gene_mapping_cache.json
  pretrained_model: weights/geneformer/default
training:
  batch_size: 8
  epochs: 30
  loss_type: mse
  lr: 0.001
  n_pseudo_bulk: 5000
  num_workers: 1
  seed: 42
  train_ratio: 0.8
  val_ratio: 0.2
